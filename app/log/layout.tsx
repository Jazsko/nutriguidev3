export const metadata = {
  title: "Nutriguide - Logg mat",
  description: "Loggfør ditt daglige matinntak",
};

export default function LogLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
